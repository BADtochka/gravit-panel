name: gravit-panel

# Panel-managed game servers are NOT part of this stack: create or attach
# LauncherDockered installations from the panel UI. Each installation runs the
# official ghcr.io/gravitlauncher/launcher image and publishes its own nginx
# facade on the Docker host (port 17549 by default), which is also what
# REMOTE_CONTROL_ENDPOINT points at.
#
# The optional `launchserver` service below is a standalone stock
# LaunchServer for simple setups. The panel does not manage it: configure it
# through its own LaunchServer.json.

x-panel-data: &panel-data
  # Absolute path on the Docker host. Must exist before starting the project
  # and must be the same inside the container — LauncherDockered Compose
  # projects use relative bind mounts resolved by the host daemon.
  DATABASE_PATH: ${PANEL_DATA_DIR:-/data/gravit-panel}/gravit-panel.sqlite
  MODULE_ARTIFACTS_DIR: ${PANEL_DATA_DIR:-/data/gravit-panel}/modules
  INSTALLATIONS_ROOT: ${PANEL_DATA_DIR:-/data/gravit-panel}/installations

services:
  # ------------------------------------------------------------------
  # API — Bun + Elysia server. Manages installations, modules, builds.
  # ------------------------------------------------------------------
  api:
    image: ${PANEL_API_IMAGE:-ghcr.io/badtochka/gravit-panel-api:latest}
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      <<: *panel-data
      # HTTP endpoint of the LauncherDockered installation facade. Docker
      # Compose maps host.docker.internal to the Docker host, where each
      # installation's nginx publishes port 17549.
      REMOTE_CONTROL_ENDPOINT: ${REMOTE_CONTROL_ENDPOINT:-http://host.docker.internal:17549}
      CREDENTIAL_ENCRYPTION_KEY: ${CREDENTIAL_ENCRYPTION_KEY:-}
      CORS_ORIGINS: ${CORS_ORIGINS:-}
      # Panel auth. Set PANEL_AUTH_MODE=discord and configure the OAuth
      # variables to enable Discord-based panel authentication.
      PANEL_AUTH_MODE: ${PANEL_AUTH_MODE:-disabled}
      PANEL_PUBLIC_URL: ${PANEL_PUBLIC_URL:-}
      PANEL_DISCORD_CLIENT_ID: ${PANEL_DISCORD_CLIENT_ID:-}
      PANEL_DISCORD_CLIENT_SECRET: ${PANEL_DISCORD_CLIENT_SECRET:-}
      PANEL_DISCORD_ALLOWED_USER_IDS: ${PANEL_DISCORD_ALLOWED_USER_IDS:-}
      PANEL_AUTH_COOKIE_SECURE: ${PANEL_AUTH_COOKIE_SECURE:-true}
    volumes:
      - type: bind
        source: ${PANEL_DATA_DIR:-/data/gravit-panel}
        target: ${PANEL_DATA_DIR:-/data/gravit-panel}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "bun -e \"const response = await fetch('http://127.0.0.1:3000/api/health'); process.exit(response.ok ? 0 : 1)\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ------------------------------------------------------------------
  # Web — nginx that serves the Vue SPA and proxies /api/ to the API.
  # ------------------------------------------------------------------
  web:
    image: ${PANEL_WEB_IMAGE:-ghcr.io/badtochka/gravit-panel-web:latest}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "127.0.0.1:${PANEL_PORT:-8080}:80"
    environment:
      # PANEL_PUBLIC_URL is the single source of truth for deriving
      # the SPA sub-path. PANEL_PUBLIC_PATH is an optional override
      # when the proxy strips a different prefix than the URL path.
      PANEL_PUBLIC_URL: ${PANEL_PUBLIC_URL:-}
      PANEL_PUBLIC_PATH: ${PANEL_PUBLIC_PATH:-}
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ------------------------------------------------------------------
  # LaunchServer (optional) — stock official image, standalone game server.
  # The panel does NOT manage this container; managed servers are
  # LauncherDockered installations. Its netty port serves the launcher
  # WebSocket API and, when fileServerEnabled, the update files directly.
  # Publish it through your game domain's HTTPS proxy with WebSocket upgrade.
  # ------------------------------------------------------------------
  launchserver:
    image: ${LAUNCHSERVER_IMAGE:-ghcr.io/gravitlauncher/launcher:v5.7.9}
    restart: unless-stopped
    stdin_open: true
    tty: true
    environment:
      ADDRESS: ${LAUNCHSERVER_ADDRESS:-localhost:9274}
      PROJECTNAME: ${LAUNCHSERVER_PROJECT_NAME:-main}
      JAVA_OPTS: ${LAUNCHSERVER_JAVA_OPTS:-}
    ports:
      - "127.0.0.1:${LAUNCHSERVER_PORT:-9274}:9274"
    volumes:
      - type: bind
        source: ${PANEL_DATA_DIR:-/data/gravit-panel}/launchserver
        target: /app/data
